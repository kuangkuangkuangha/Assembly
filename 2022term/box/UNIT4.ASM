.386
DATA SEGMENT USE16

INPUTX DB 'PLEASE INPUT X: $'

INPUTY DB 'PLEASE INPUT Y: $'
X DB ?
Y DB ?
A DB ?
HIGH DB

ERR1   DB 'INPUT VOER 127$'
ERR2   DB 'INOUT LESS -128$'

DATA ENDS

CODE SEGMENT
ASSUME DS:DATA, CS:CODE, SS:STACK

START:  MOV AX, DATA
        MOV DS, AX

        MOV DX, OFFSET INPUTX
        MOV AH, 9
        INT 21H

        MOV AH, 1
        INT 21H

        CMP AL, 127

ERR:    

CODE ENDS
        END START        
